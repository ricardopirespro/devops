Algoritmo "teste09"


Var


   INDICE, SOMA, K: INTEIRO

Inicio

   INDICE <- 6
   SOMA <- 0
   K <- 0

   Enquanto (K < INDICE) faca
      K <- K + 1
      SOMA <- SOMA + K

      escrevaL (K)
   FimEnquanto

   EscrevaL ("valor variavel soma: ",SOMA)


Fimalgoritmo